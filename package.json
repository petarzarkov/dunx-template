{
  "name": "dunx-template",
  "version": "0.1.0",
  "private": true,
  "description": "A production-shaped starter for dunx: validated config, structured logging, SQLite via drizzle, cursor pagination, OpenAPI, Docker and CI.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/main.ts",
    "start": "bun src/main.ts",
    "worker": "bun src/worker.ts",
    "start:dist": "bun dist/main.js",
    "build": "bun ./scripts/build.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint --fix src scripts e2e",
    "lint:check": "oxlint src scripts e2e",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "test": "bun test",
    "test:unit": "bun test .test.ts",
    "test:int": "bun test .spec.ts",
    "test:cov": "bun test --coverage",
    "test:e2e": "bun test --preload ./e2e/setup/preload.ts $(find ./e2e -name '*.e2e.ts')",
    "mig:gen": "bun --env-file=.env drizzle-kit generate",
    "mig:run": "bun --env-file=.env ./scripts/migrate.ts",
    "db:studio": "bun --env-file=.env drizzle-kit studio",
    "db:drop": "bun ./scripts/db-drop.ts",
    "seed": "bun --env-file=.env ./scripts/seed.ts",
    "gen:openapi": "bunx dunx-openapi ./src/openapi.config.ts",
    "gen:env:docs": "bun ./scripts/gen-env-docs.ts"
  },
  "dependencies": {
    "@dunx/auth": "3.8.1",
    "@dunx/core": "3.8.1",
    "@dunx/http": "3.8.1",
    "@dunx/infra": "3.8.1",
    "@dunx/openapi": "3.8.1",
    "@dunx/transform": "3.8.1",
    "@scalar/api-reference": "1.68.0",
    "better-auth": "1.6.25",
    "bullmq": "6.3.4",
    "drizzle-orm": "0.45.2",
    "ioredis": "6.0.0",
    "swagger-ui-dist": "5.32.14",
    "zod": "4.5.4"
  },
  "devDependencies": {
    "@dunx/testing": "3.8.1",
    "@types/bun": "1.4.1",
    "drizzle-kit": "^0.31.10",
    "oxfmt": "0.66.0",
    "oxlint": "1.81.0",
    "oxlint-tsgolint": "7.0.2001",
    "typescript": "7.0.2"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
